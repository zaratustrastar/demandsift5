name: Diagnose fully enriched TVCP zero

on:
  push:
    branches:
      - diagnostic/runtime-queue-20260814
    paths:
      - .github/workflows/diagnose-tvcp-accepted-zero-20260815.yml

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 5
    steps:
      - name: Diagnose fresh TVCP scan safely
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo -n /usr/local/sbin/demandsift-release diagnose scan_7d07ad2b4075431b87b080424ae2b18c
