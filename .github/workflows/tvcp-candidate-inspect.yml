name: TVCP stored candidate inspection

on:
  push:
    branches:
      - diagnostic/tvcp-candidate-inspect-20260814

jobs:
  inspect-tvcp:
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    steps:
      - name: Inspect narrow runner privileges
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo -n -l
          sudo -n /usr/local/sbin/demandsift-release diagnose scan_125da85d566b4c2ba10bab84193709b1
